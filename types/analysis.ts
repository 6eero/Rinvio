export interface GlobalStats {
  total: number;
  favoriteCrag: string;
  successRate: number;
  avgAttempts: number;
}

export interface LeadStats {
  maxGrade: string;
  total: number;
  closed: number;
  completionRate: number;
  avgAttempts: number;
}
